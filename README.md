# mediasite

## To run:

- Clone the repo (natch)
- `cd mediasite`
- `composer install`
- `npm install`
- `grunt`
- `grunt watch` (to compile scss and jsx on the fly)

## Requirements:

- Node/npm
- Composer
