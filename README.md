# mediasite

## To run:

- Clone the repo (natch)
- `cd mediasite`
- `composer install`
- `npm install`
- `webpack`
- `webpack -w` (to compile everything on the fly!)

## Requirements:

- PHP >= 5.4
- Node/npm
- Composer
